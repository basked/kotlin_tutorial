1) Перечисления нельзя расширить после определения
2) В опереаторе when можно писать без else - так как перечисления - это конечный набор
3) У пересичления могут быть конструкторы
   enum class Color
       (val r:Int,g:Int,b:Int){
       RED(255,0,0), GREEN(0,255,0), BLUE(0,0,255)
   }
4) У перечисления могут  быть произвольные функции
5) Перечесления в основном используються в условных опереаторах if, when...